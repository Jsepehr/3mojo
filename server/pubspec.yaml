name: threemojo_server
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  dart_frog: ^1.1.0
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  mocktail: ^1.0.0
  test: ^1.0.0
